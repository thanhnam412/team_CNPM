generator kysely {
  provider = "prisma-kysely"
  output   = "../../src/database"
  fileName = "types.ts"
}

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}
