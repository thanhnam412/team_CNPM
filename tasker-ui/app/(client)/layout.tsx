import { RoleLayout } from "@/components/aitasker/role-layout";
export default function Layout({ children }: { children: React.ReactNode }) { return <RoleLayout role="client">{children}</RoleLayout>; }
